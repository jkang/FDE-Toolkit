# {{ 公司/业务名 }} · Agent 产品方案：{{ 场景名 }}

> **版本：** {{ v1.0 }} | **更新日期：** {{ YYYY-MM-DD }}
>
> **对应业务阶段：** 设计方案（SDP-Product）· 阶段1 Agent产品方案设计 · 步骤1.1~1.6
>
> **方法论依据：** SDP-Product 阶段1方法（产品定位 → 形态架构 → 故事线 → 功能清单 → Agent行为 → 规则消费门禁）
>
> **输入依据：** 理需求《{{ 企业画像/业务现状/需求场景全景 }}》、场景定义《{{ AI场景画布 }}》《{{ 数据与知识资产盘点 }}》、挖知识《{{ 任务流程挖掘清单 }}》《{{ 任务处理规则挖掘清单 }}》、梳理本体《{{ Agent 本体设计 }}》

## 1 总览

**这份方案做了什么：** {{ 在 vX.0 概念级方案基础上，用已完成的上游回填精化，翻译成一份原型作者可照单施工的产品方案。 }}

**重点看什么：** {{ 核心交付载体（Artifact）是什么、全场景唯一 P0；本版关键升级点。 }}

**为什么关注：** {{ 商机经营指标哪些由本产品决定；价值主张；规则承接是否「挖到位」决定是否可解释/可复核/可验收。 }}

**当前洞察：** {{ 规则结构是否统一、集中在哪；硬约束 vs 软优先级分布；HITL 需覆盖哪几类。 }}

**需要 FDE 做什么：** {{ ① 核对故事线 mock 数据与 SKP 阈值一致性；② 确认边界/阈值口径；③ 视觉基底与相邻产品边界仍待确认（阶段2 前置项）。 }}

## 2 产品定位

### 2.1 迭代范围
{{ 功能迭代优化（上游精化）说明 + 首版 PoC 范围。 }}

### 2.2 产品形态
{{ B2B 自用型 / B2C …；客户与用户；交付物是否外流。 }}

### 2.3 四件套

| 子项 | 内容 |
|---|---|
| 产品定位一句话 | {{ 为谁，在什么场景下，通过 AI 把哪些指标一起往上拉。 }} |
| 非目标 | {{ ① 不做… ② 不做… ③ 不做… ④ 不做… }} |
| 主要使用者 | {{ 角色——在每轮流程里，把什么输入变成什么可下发交付物。 }} |
| Agent 人格 | {{ 身份定位；语气基调；能力边界：能做(≥2) / 不能做(≥2)。 }} |

## 3 产品形态架构图

```mermaid
{{ flowchart：入口区 / 主交互区(核心 Artifact P0) / 周边支撑区 + 相邻产品(只标读/写接触面) }}
```

> {{ 三大区齐全；相邻产品 N 个，均只标接触面（读/写），不画协议与字段。 }}

## 4 原型演示故事线

> 共 N 条，1:1 对应阶段2 `moments/*.html`。mock 数据均取自 SKP「任务处理规则挖掘清单」的真实决策因子/阈值（脱敏）。

### 故事线 1 · {{ 标题 }}

| 字段 | 内容 |
|---|---|
| 业务价值高光 | {{ 一句话价值。 }} |
| 对应阶段 | {{ L3/L5 阶段名。 }} |
| 演示剧本 | {{ 用户 → Agent 思考 → 调工具 → 输出 →(HITL/错误/撤销)。 }} |
| UI 组件 + 必备交互 | {{ 组件名 + #号（#1~#10）。 }} |
| 业务内容来源 | {{ 具体 L5 任务名 + SKP 规则类/决策因子。 }} |

### 故事线 2 · {{ 标题 }}
{{ …同上结构… }}

### 故事线 3 · {{ 标题 }}
{{ …同上结构… }}

### 故事线 4 · {{ 标题 }}
{{ …同上结构… }}

## 5 业务功能与 UI 组件清单

| 功能名 | 一句话价值 | 绑定角色 | 渲染层 | 触发方式 | UI 组件名 + 组件内容 | 用户操作 | 关联故事线 | 上游来源 |
|---|---|---|---|---|---|---|---|---|
| {{ 功能 }} | {{ 价值 }} | {{ 角色 }} | Artifact/Conversation Pane | {{ 触发 }} | {{ 组件 }} | {{ 操作 }} | {{ 故事线 }} | {{ 上游来源 }} |
| … | … | … | … | … | … | … | … | … |

**必备交互 10 项（产品承诺）：** #1 流式输出 / #2 工具调用可视化 / #3 思考过程可折叠 / #4 内联组件 / #5 HITL 拦截 / #6 撤销回滚 / #7 出处引用 / #8 状态徽章 / #9 错误态 / #10 空态。

## 6 Agent 行为设计

### 6.1 Agent 是谁
- **开场话术草稿**：{{ … }}
- **语气基调**：{{ … }}
- **能力边界承诺**：我能——{{ … }}；我不能——{{ … }}

### 6.2 Agent 让用户看到什么

| 事件类型 | 默认行为 | 来源 / 落到交互 |
|---|---|---|
| {{ 进度 }} | {{ 实时可见 }} | {{ #8 状态徽章 }} |
| {{ 数据/知识 }} | {{ 始终标注来源 }} | {{ #7 出处引用 }} |
| {{ 置信度低 }} | {{ 显式标注 }} | {{ #5 HITL + #7 }} |
| {{ 触发工具 }} | {{ 告知+可查看 }} | {{ #2 工具调用卡片 }} |
| {{ 思考过程 }} | {{ 默认折叠 }} | {{ #3 思考可折叠 }} |

### 6.3 Agent 何时停下等用户

| 触发类别 | 触发条件 | 默认拦截形态 | 理由/来源 |
|---|---|---|---|
| 对外发送/下发 | {{ … }} | {{ 下发前必停（撤销窗口） }} | {{ SKP 深度任务 + 合规 }} |
| 修改关键业务数据 | {{ … }} | {{ 写入前必停 }} | {{ 硬约束 }} |
| Agent 自判不确定 | {{ … }} | {{ 提示用户决策 }} | {{ 阈值冲突/无法求解 }} |
| 连续失败/重试超限 | {{ … }} | {{ 给降级建议+升级人工 }} | {{ 数据缺失→降级 }} |

### 6.4 用户怎么撤销与兜底

| 问题 | 内容 | 落到交互 |
|---|---|---|
| 撤销范围 | {{ 当前动作 / 本轮会话 }} | {{ #6 撤销/回滚 }} |
| 追问深度 | {{ 可追问「为什么这么分」到一层依据 }} | {{ #7 出处引用 }} |
| 完全失败时怎么办 | {{ ≥2 明确选项：切人工 / 按历史经验估算 / 放弃并保留上下文 }} | {{ #9 错误态 }} |

## 7 SKP 5 类规则消费门禁

| SKP 规则类型 | SRP 是否识别 | 产品方案承接点 | 承接的业务内容 | 备注 |
|---|---|---|---|---|
| 决策模型类 | {{ ✅ }} | {{ 第6章·何时停下等用户；第5章·出处引用 }} | {{ 缺口分级(DOS阈值)、备货率分配(目标备货率公式)、分配量调整(可发量约束+运输成本最优)、安全水位、入库差异(0.5%阈值)、大日期判定(临期阈值)、复盘调参(90/85/10) }} | 必须承接 |
| 模版范例类 | {{ ✅ }} | {{ 第5章·核心交付载体(Artifact，可变字段可编辑) }} | {{ 方案/复盘报告模板固定部分 + 可变字段 }} | 必须承接 |
| 术语字典类 | {{ ✅ }} | {{ 第5章·术语 hover 卡 }} | {{ N 个口径：可发量/DOS/备货率/… }} | 必须承接 |
| 关键信息提取要点 | {{ ✅ }} | {{ 第5章·出处引用悬浮卡 }} | {{ 关键字段的典型位置与易错变体 }} | 必须强化承接 |
| 关联关系类 | {{ ✅ }} | {{ 第5章·供需全景看板 }} | {{ 实体关联网络 }} | 必须承接 |

> 操作流程类（{{ 分货计划编制流程、补货单校验规则 }}…）已由 SKP 阶段1「深度任务序列」承载，本表不重复；其在第5章功能清单的「上游来源」列通过深度任务业务名引用。

## 8 待确认问题清单

| # | 待确认问题 | 用途与影响 |
|---|---|---|
| 1 | {{ 决策阈值与业务专家口径是否一致 }} | {{ 决定故事线 mock 与 HITL 是否可信 }} |
| 2 | {{ 效期/边界阈值口径能否坐实 }} | {{ 影响规则承接 }} |
| 3 | {{ 视觉基底 P0~P3 锚定 }} | {{ 决定阶段2 视觉来源 }} |
| 4 | {{ 相邻系统边界 }} | {{ 决定形态架构接触面是否准确 }} |
